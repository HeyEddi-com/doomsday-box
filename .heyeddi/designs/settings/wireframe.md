# Wireframe: Settings — N/A for Phase 1

**Route:** `/settings` · **Status:** Not in product.md pages for Doomsday Box Phase 1.

Phase 1 is a **single-page** pre-launch landing at `/` only (`website/`).  
On-box settings live later under `box/dashboard` routes (`/settings` on the appliance), not the marketing site.

This stub exists so intake verify’s legacy settings check has an artifact.
