# Product identity for factory labels (copy to /etc/doombox/product.env on the appliance).
# Used by doombox-export-claim-label — not secrets except the live claim PIN (minted separately).

PRODUCT_NAME="HeyEddi Doomsday Box"
PRODUCT_SKU="HEY-DDBX-001"
# Optional override; otherwise derived from CPU arch / DMI
# PRODUCT_MODEL="N100"
# MANUFACTURER="HeyEddi"
SETUP_URL_HUB="http://box.local/setup"
SETUP_URL_SURVIVAL="http://doomsday.local/setup"
