# Dev overrides — bind-mount sources (optional)
services:
  api:
    volumes:
      - ../api:/app:ro
      - ${STORAGE_ROOT:-/mnt/storage}:/mnt/storage:rw
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8090", "--reload"]
