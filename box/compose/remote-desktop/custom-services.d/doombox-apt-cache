#!/usr/bin/env bash
# Background: keep extra-package manifest + .deb cache fresh.
set -uo pipefail
SYNC="/usr/local/share/doombox-desktop-help/doombox-sync-apt-cache.sh"
sleep 15
while true; do
  if [[ -x "${SYNC}" ]]; then
    "${SYNC}" || true
  fi
  sleep 30
done
